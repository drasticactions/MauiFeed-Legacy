<UserControl
    x:Class="MauiFeed.WinUI.Views.RefreshView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MauiFeed.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="300" Height="100" Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}">
        <Grid.CornerRadius>10</Grid.CornerRadius>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ProgressRing Margin="25,0,0,0" Grid.Column="0" />
        <StackPanel Spacing="5" Margin="25,0,25,0" VerticalAlignment="Center" Grid.Column="1">
            <TextBlock x:Name="RefreshLabel" TextWrapping="NoWrap" TextTrimming="WordEllipsis" Text="" />
        </StackPanel>
    </Grid>
</UserControl>
